apiVersion: v1
kind: Secret
metadata:
  name: adrex-api-secret
  namespace: adrex
type: Opaque
stringData:
  # CHANGE THIS — generate with: openssl rand -base64 32
  API_KEYS: "CHANGE-ME-generate-with-openssl-rand-base64-32"
