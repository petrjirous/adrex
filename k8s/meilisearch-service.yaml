apiVersion: v1
kind: Service
metadata:
  name: adrex-meilisearch
  namespace: adrex
  labels:
    app.kubernetes.io/name: meilisearch
    app.kubernetes.io/part-of: adrex
    app.kubernetes.io/component: search
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: meilisearch
    app.kubernetes.io/part-of: adrex
    app.kubernetes.io/component: search
  ports:
    - name: http
      port: 7700
      targetPort: http
