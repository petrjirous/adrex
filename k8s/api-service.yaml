apiVersion: v1
kind: Service
metadata:
  name: adrex-api
  namespace: adrex
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: adrex
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: adrex
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
