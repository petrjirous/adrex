apiVersion: v1
kind: Secret
metadata:
  name: adrex-meilisearch-secret
  namespace: adrex
type: Opaque
stringData:
  # CHANGE THIS — generate with: openssl rand -base64 32
  MEILI_MASTER_KEY: "CHANGE-ME-generate-with-openssl-rand-base64-32"
