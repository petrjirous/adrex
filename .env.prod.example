# Domain for Caddy auto-HTTPS
DOMAIN=adrex.petrjirous.cz

# Meilisearch master key (generate: openssl rand -base64 32)
MEILI_MASTER_KEY=change-me-to-a-strong-random-key

# API keys for client authentication (comma-separated)
# First key is the public demo key (embedded in demo page)
API_KEYS=demo-public-key,your-private-admin-key
